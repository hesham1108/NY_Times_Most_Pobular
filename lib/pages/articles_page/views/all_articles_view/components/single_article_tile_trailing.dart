import 'package:flutter/material.dart';

class SingleArticleTileTrailing extends StatelessWidget {
  const SingleArticleTileTrailing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.arrow_forward_ios_sharp,
      color: Color(0xFF9D9D9D),
    );
  }
}
